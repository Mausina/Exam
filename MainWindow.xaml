<Window x:Class="GymApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Main Window" Height="500" Width="800" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Members" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />
            <ListBox x:Name="lbxMembers" SelectionChanged="lbxMembers_SelectionChanged" DisplayMemberPath="DisplayName" />
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Member Details" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />
            <Grid Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtId" Grid.Row="0" Grid.Column="0" Margin="5" />
                <TextBlock x:Name="txtFirstName" Grid.Row="0" Grid.Column="1" Margin="5" />
                <TextBlock x:Name="txtLastName" Grid.Row="0" Grid.Column="2" Margin="5" />

                <TextBlock x:Name="txtContact" Grid.Row="1" Grid.Column="0" Margin="5" />
                <TextBlock x:Name="txtMembership" Grid.Row="1" Grid.Column="1" Margin="5" />
                <TextBlock x:Name="txtDOB" Grid.Row="1" Grid.Column="2" Margin="5" />
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Training Sessions" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="0,0,15,0" />
                <Button Content="Add Session" x:Name="btnAddSession" Click="btnAddSession_Click" />
            </StackPanel>

            <TextBlock x:Name="txtNoSessions" Text="No training sessions available." Visibility="Collapsed" Foreground="Gray" />

            <ListBox x:Name="lbxSessions" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="2" Padding="5">
                            <StackPanel>
                                <TextBlock Text="{Binding SessionDate, StringFormat='dd/MM/yyyy HH:mm'}" FontWeight="Bold" />
                                <TextBlock Text="{Binding SessionType}" />
                                <TextBlock Text="{Binding DurationMinutes, StringFormat='{}{0} min'}" />
                                <TextBlock Text="{Binding DisplayNotes}" Foreground="DarkGray" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>